apiVersion: v1
kind: Service
metadata:
  name: cicd-test-svc
  namespace: default
spec:
  type: NodePort
  selector:
    app: cicd-test-app
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007
